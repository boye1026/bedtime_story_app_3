name: bedtime_story_app
# AI 睡前故事生成器 - Flutter 移动应用
description: AI 睡前故事应用 - 为孩子定制专属的睡前故事
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: 3.8.5
  shared_preferences: 2.2.2
  path_provider: 2.1.1
  dio: 5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 3.0.1

flutter:
  uses-material-design: true
